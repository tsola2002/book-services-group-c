server:
  port: 7003

spring:
  application:
    name: recommendationservice

  datasource:
    url: jdbc:postgresql://localhost:5432/bookrecommendation
    username: "postgres"
    password: "myproject"
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect